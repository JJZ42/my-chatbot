# 📊 前后对比差异报告

> **V1.0（原版）→ V2.0（改进版）→ V2.1（文件上传 + 美化）**  
> **Commit 范围**：`c462c42` → `29718d2`  
> **文件变更**：`app.py`（+350行 / -30行）、`requirements.txt`（+2行）

---

## 一、总览

| 维度 | V1.0 原版 | V2.0 改进版 | V2.1 最新版 |
| --- | --- | --- | --- |
| 知识库数量 | 1个（覆盖） | N个（共存） | N个（共存） |
| 刷新持久化 | ❌ 刷新丢失 | ✅ 刷新保留 | ✅ 刷新保留 |
| 知识库切换 | 不支持 | selectbox | selectbox |
| 对话记忆 | ❌ 独立提问 | ✅ 上下文 | ✅ 上下文 |
| 中文检索 | ❌ 全0分 | ✅ char_wb | ✅ char_wb |
| 文件上传 | ❌ | ❌ | ✅ PDF/Word 拖拽 |
| 页面美化 | ❌ 白底黑字 | ❌ 白底黑字 | ✅ 暗色玻璃+动画 |
| 登录页 | 简陋表单 | 简陋表单 | ✅ 卡片+渐变+Enter |
| 聊天气泡 | 无动画 | 无动画 | ✅ 滑入交错动画 |

---

## 二、逐模块对比

### 2.1 数据模型

```
V1.0                              V2.0
┌────────────────────┐            ┌─────────────────────────┐
│ st.session_state   │            │ st.session_state        │
│   .kb = (vec,      │            │   .knowledge_bases = {  │
│          matrix,   │            │       "Python入门": [...],│
│          texts)    │            │       "C语言基础": [...],│
│                    │            │       "我的知识": [...] │
│   (单个TF-IDF对象)  │            │     }                   │
└────────────────────┘            │   .active_kb = "Python入门"│
                                   │   .messages_rag = [...]  │
                                   └─────────────────────────┘
```

### 2.2 知识库函数

| | V1.0 | V2.0 |
| --- | --- | --- |
| 函数名 | `create_knowledge_base(texts)` | `compute_tfidf(texts_tuple)` |
| 缓存 | 无 | `@st.cache_resource` |
| 参数 | `texts: list` | `texts_tuple: tuple`（hashable） |
| 分词器 | `TfidfVectorizer()` | `TfidfVectorizer(analyzer='char_wb', ngram_range=(1,3))` |

**关键差异 — 中文分词：**

```python
# V1.0：默认分词 → 中文挤成1个token → 检索全0分
vectorizer = TfidfVectorizer()

# V2.0：逐字分词 + 1~3字ngram → 正确匹配中文
vectorizer = TfidfVectorizer(analyzer='char_wb', ngram_range=(1, 3))
```

**效果对比（查询 "list是什么"，知识库有12条Python知识）：**

| | V1.0 | V2.0 |
| --- | --- | --- |
| No.1 | 异常处理 (score: **0.0000**) | for循环 (score: **0.3093**) |
| No.2 | 文件读取 (score: **0.0000**) | 列表list定义 (score: **0.2606**) |
| No.3 | pip安装 (score: **0.0000**) | pip安装 (score: **0.1863**) |
| 结论 | 🔴 完全随机 | 🟢 正确匹配 |

### 2.3 侧边栏 UI

```
V1.0                              V2.0
┌────────────────────┐            ┌─────────────────────────┐
│ 📄 知识库管理        │            │ 📄 知识库管理              │
│                    │            │                         │
│ 方式一：粘贴文本      │            │ 📌 当前知识库             │
│ [text_area]        │            │ [selectbox: Python入门 ▾] │
│ [✅ 加载我的知识]    │            │ 共45条知识    [🗑️]       │
│                    │            │ ─────────────────────── │
│ 方式二：加载示例      │            │ 方式一：粘贴文本           │
│ [Python] [C语言]   │            │ 知识库名称: [________]  │
│                    │            │ [text_area]            │
│ 📌 知识库已就绪      │            │ [✅ 加载我的知识]         │
│                    │            │ ─────────────────────── │
│ [🗑️ 清空对话]       │            │ 方式二：加载示例           │
└────────────────────┘            │ [Python入门] [C语言基础] │
                                   │ ─────────────────────── │
                                   │ [🗑️ 清空全部知识库]       │
                                   │ [💬 清空对话]           │
                                   └─────────────────────────┘
```

### 2.4 主对话区

```python
# V1.0：每次只发当前问题（无对话记忆）
messages=[{"role": "user", "content": prompt}]

# V2.0：发送完整对话历史 + 当前RAG增强问题
chat_messages = [
    {"role": "user", "content": "指针是什么"},       # 历史Q1
    {"role": "assistant", "content": "指针是..."},   # 历史A1
    {"role": "user", "content": "RAG上下文 + 它和引用的区别"}  # 当前Q2
]
```

**效果对比：**

```
V1.0                              V2.0
─────────────────────────────────────────────────
User: 指针是什么                    User: 指针是什么
AI: 指针是存储地址的变量...          AI: 指针是存储地址的变量...

User: 它和引用的区别                 User: 它和引用的区别
AI: 🤷 不知道"它"指什么             AI: 指针和引用的区别是：
    (因为没看到上文)                    指针存储地址，引用是别名...
                                    (理解了"它"=指针)
```

### 2.5 按钮行为

| 按钮 | V1.0 | V2.0 |
| --- | --- | --- |
| 📥 Python入门 | 覆盖 `st.session_state.kb` | 存入 `knowledge_bases["Python入门"]` |
| 📥 C语言基础 | 覆盖 `st.session_state.kb` | 存入 `knowledge_bases["C语言基础"]` |
| ✅ 加载我的知识 | 覆盖、无名称 | 可命名、存入 dict |
| 🗑️ 清空对话 | ✓ | ✓ |
| 🗑️ 清空全部知识库 | 无 | ✨ 新增 |
| 🗑️ 删除当前知识库 | 无 | ✨ 新增 |

---

## 三、代码变更量

```
 app.py | 85 insertions(+), 18 deletions(-)
 
 变更明细：
 ┌─────────────────────────────────────┬──────┐
 │ 变更内容                              │ 行数  │
 ├─────────────────────────────────────┼──────┤
 │ 知识库函数重写                         │ +8   │
 │ (create_kb → compute_tfidf + cache)   │      │
 ├─────────────────────────────────────┼──────┤
 │ 侧边栏 UI 重写                         │ +48  │
 │ (多KB管理 + selectbox + 删除)          │      │
 ├─────────────────────────────────────┼──────┤
 │ 主对话区重写                           │ +20  │
 │ (多KB读取 + 对话记忆 + cache)          │      │
 ├─────────────────────────────────────┼──────┤
 │ TF-IDF 分词修复                        │ +4   │
 │ (char_wb + ngram 中文兼容)            │      │
 ├─────────────────────────────────────┼──────┤
 │ 侧边栏按钮更新                         │ +5   │
 │ (存入 dict 而非覆盖)                   │      │
 └─────────────────────────────────────┴──────┘
```

---

## 四、部署影响

| | V1.0 | V2.0 |
| --- | --- | --- |
| 依赖变更 | 无 | 无（仍用 scikit-learn） |
| `.env` 变更 | 无 | 无 |
| `requirements.txt` | 不变 | 不变 |
| Streamlit Cloud 兼容 | ✓ | ✓ |
| 向后兼容 | - | 首次加载知识库为空，需重新加载 |

---

## 五、V2.0 → V2.1 新增

### 文件上传

```python
# 新增函数
extract_text_from_pdf(file_bytes)   # PyPDF2 提取
extract_text_from_docx(file_bytes)  # python-docx 提取
split_into_chunks(text, min_len)    # 智能分段
```

| 支持格式 | PDF (.pdf) / Word (.docx) |
| --- | --- |
| 文本提取 | PyPDF2 逐页提取 / python-docx 逐段提取 |
| 分段策略 | 双换行 → 段落级；>500字 → 句号级 |
| 阈值 | 最小20字（段落）、最小3字（句子） |
| 命名 | 自动取文件名（去后缀） |

### 页面美化

| 效果 | CSS 关键技术 |
| --- | --- |
| 浮动 Logo | `@keyframes float` + `@keyframes glow` |
| 渐变标题 | `background-clip: text` + `linear-gradient` |
| 玻璃侧边栏 | `backdrop-filter: blur(16px)` |
| 气泡滑入 | `@keyframes slideIn` + `nth-child` 延迟 |
| 登录卡片 | `backdrop-filter` + `border-radius: 24px` |

### 依赖新增

```
PyPDF2>=3.0.0       # PDF 文本提取
python-docx>=1.0.0  # Word 文本提取
```

---

## 六、已知限制

| 限制 | 说明 |
| --- | --- |
| 刷新持久化范围 | `st.session_state` 级别 — 服务器重启后丢失 |
| 中文分词精度 | `char_wb` 是字符级，不如 jieba 等专业分词器 |
| 知识库大小 | 内存存储，超大知识库（>1000条）可能卡顿 |
| 对话记忆开销 | 每轮都发全部历史，长对话 token 消耗递增 |
| PDF 扫描件 | 图片型 PDF 无法提取文字（需 OCR） |
| 旧版 Word | 仅支持 .docx，不支持 .doc |
